{% extends 'base.html.twig' %}

{% block title %}Admin - Nouveau projet{% endblock %}

{% block main %}
<div class="container py-4" style="max-width: 900px;">

  <div class="d-flex justify-content-between align-items-center mb-3">
    <div>
      <h1 class="h4 mb-1">merhba</h1>
      <div class="text-muted small">Ajoutez un projet de crowdfunding et publiez-le.</div>
    </div>

    <a class="btn btn-outline-secondary" href="{{ path('app_admin_project_index') }}">
      ← Retour à la liste
    </a>
  </div>

  <div class="card shadow-sm">
    <div class="card-body p-4">
      {{ include('admin_project/_form.html.twig', { button_label: 'Créer le projet' }) }}
    </div>
  </div>

</div>
{% endblock %}
